import LabelIndicatorCarousel from '@/components/ui/label_indicator_carousel'
import Divider from '@/components/ui/divider'
import Heading from '@/components/ui/heading'
import { Table } from '@/components/ui/table'
import InlineYoutubePreview from '@/components/ui/inline-youtube-preview'

<div className="max-w-[560px] mx-auto">

<span className="block">
  <InlineYoutubePreview videoId="xqcpR1u-OcA" start={8}>AlphaGrill</InlineYoutubePreview>
  {' '}is an grilling robot for commercial kitchens like McDonald's.
</span>

For 3–4 years, the team shipped without a product or design owner. As the company prepared the next version, I proposed and led a redesign of robot interaction, working with the industrial designer and engineers through implementation.

<Heading title="Designing the grill workflow" year="2025 - 2026" tocLabel="Grill workflow" />

Commercial kitchens move fast and AlphaGrill automates one task: grilling patties. The bot cooks and unloads patties in a few minutes, letting one line cook manage the grill line alongside other tasks. The redesign needed to support cooks managing multiple tasks in parallel.

The robot runs through many internal states. Interviews and observation showed that for chefs, these collapse into two phases: setup and cooking. The interface mirrors this model.

<Table
  columns={['Phase', 'What chefs need', 'Interface response']}
  rows={[
    ['Setup', '"What do I need to start cooking?"', 'Simple controls to start'],
    ['Cooking', '"When will this batch finish?"', 'Clear progress display'],
  ]}
  highlightColumn={0}
/>

<div className="w-screen relative left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] my-20">
  <LabelIndicatorCarousel
    showIndicators={false}
    items={[
      {
        label: "GRILL",
        caption: "The gauge shows degrees remaining until ready, rather than the current temperature<sup>1</sup>",
        videoUrl: "https://f5uskgwhyu2fi170.public.blob.vercel-storage.com/grill-1.mov",
        imageSizePercent: 85,
        imagePosition: { top: "center", left: "center" },
      },
      {
        label: "GRILL",
        caption: "Recipe selection uses taps instead of swipes, reducing failed inputs with gloved hands<sup>2</sup>",
        videoUrl: "https://f5uskgwhyu2fi170.public.blob.vercel-storage.com/grill-2.mov",
        imageSizePercent: 85,
        imagePosition: { top: "center", left: "center" },
      },
    ]}
  />
</div>

Two decisions anchored the redesign:

**Showing readiness, not temperature<sup>1</sup>** Chefs didn't want to know the current temperature — they wanted to know when the robot would be ready. I replaced numeric readings with a circular gauge showing degrees remaining, matching the question they were actually asking.

**Tapping instead of swiping<sup>2</sup>** Swipe-based recipe selection failed often with oily gloved hands. I evaluated swipe vs. tap against two constraints: *reliability* (tap needs position only; swipe needs position + direction) and *reach* (frequent actions belong in the lower screen region).


<Heading title="Designing safe cleaning controls" year="2026" tocLabel="Cleaning controls" />

Kitchen robots require regular cleaning. Oil mist and food debris quickly build up inside the grill. If not cleaned properly, the robot may cook unevenly or stop during operation. Cleaning must be simple and safe for chefs.

Effective cleaning requires moving the grill into specific positions. Different areas become accessible only when the grill is repositioned.

Through observation and testing of the cleaning process, three positions emerged as the most effective: cleaning the inner wall, the griddle surface, and the underside of the grill. These positions form the foundation of the cleaning workflow.

The interface centers on repositioning the grill. Employees choose the area they are cleaning, and the robot moves to the correct position.

<div className="w-screen relative left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] my-20">
  <LabelIndicatorCarousel
    showIndicators={false}
    items={[
      {
        label: "GRILL",
        caption: "Choosing the cleaning area and repositioning the grill",
        videoUrl: "https://f5uskgwhyu2fi170.public.blob.vercel-storage.com/clean-1.mov",
        imageSizePercent: 85,
        imagePosition: { top: "center", left: "center" },
      },
    ]}
  />
</div>

Safety was a key concern. Moving grill parts can cause burns or injuries if triggered accidentally. To prevent this, the interface requires a second confirmation before the robot moves.

The slider provides the sense of physical control, while the confirmation step prevents unintended actions.

</div>
